version: "3.9"

services:
  loteria:
    build: .
    image: loteria:latest
    container_name: loteria-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    networks:
      - loteria-network
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--quiet",
          "--tries=1",
          "--spider",
          "http://localhost:3000/",
        ]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
    volumes:
      - /var/log/nginx:/var/log/nginx:rw

  telegram-bot:
    build:
      context: ./telegram-bot
      dockerfile: Dockerfile
    image: loteria-bot:latest
    container_name: loteria-telegram-bot
    env_file:
      - ./telegram-bot/.env
    networks:
      - loteria-network
    restart: unless-stopped

networks:
  loteria-network:
    driver: bridge
